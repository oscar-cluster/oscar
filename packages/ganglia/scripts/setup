#!/bin/sh
#
# Copyright (c) 2001-2003 The Trustees of Indiana University.  
#                         All rights reserved.
# 
# This file is part of the OSCAR software package.  For license
# information, see the COPYING file in the top level directory of the
# OSCAR source distribution.
#
# $Id: setup,v 1.3 2003/01/21 21:55:46 jsquyres Exp $
#

#
# Get the name of the libpng RPM in the extras directory
#

cd "$OSCAR_PACKAGE_HOME/extras"
libpng_name="`/bin/ls libpng*rpm`"

#
# Remove any prior instance of it from the RPMS directory
#

cd "$OSCAR_PACKAGE_HOME/RPMS"
rm -f $libpng_name

#
# If we're on a Mandrake system, sym link it from extras to RPMS
#

grep Mandrake /etc/issue 2>&1 > /dev/null
if test "$?" = "0"; then
    ln -s ../extras/$libpng_name .
    echo "Created sym link for libpng in ganglia/RPMS directory"
fi

#
# All done
#

exit 0
