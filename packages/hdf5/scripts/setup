#!/bin/sh

installable=1

if [ `/bin/uname -m` == 'ia64' ]; then
  installable=0
  echo "HDF5 will not be installed due to ia64 incompatibility"
else
  echo "HDF5 will be installed"
fi

perl -pi.bak -e "s/installable\>./installable>$installable/g" \
$OSCAR_HOME/packages/hdf5/config.xml
